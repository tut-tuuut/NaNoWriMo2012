\ProvidesPackage{tutie}

\usepackage[utf8x]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[french]{babel}
\usepackage{charter}  % <3

\usepackage[pdfborder={0 0 0}]{hyperref}

\renewcommand{\thesection}{\arabic{section}}

% \go{X} stands for « go to paragraph X »
\newcommand{\go}[1]{$\rightarrow$~\textbf{\ref{#1}}}

% creates sections without true title and a "true" label
\let\sectionOld = \section
\renewcommand{\section}[1]{\sectionOld{------------------------------ \texttt{[#1]}} \label{#1}} %debug
%\renewcommand{\section}[1]{\sectionOld{------------------------------------------} \label{#1}} %sharing

% environment for « now, what ? » paragraphs.
\newenvironment{nw}
{\begin{quote} \small \textbf{\textit{Et maintenant…}} \begin{itemize}}
{\end{itemize} \normalsize \end{quote}}
\newcommand{\bnw}{\begin{nw}}
\newcommand{\enw}{\end{nw}}